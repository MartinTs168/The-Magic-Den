@model SubCategoryViewModel


@{
    ViewData["Title"] = "Delete Sub Category";
}

<h2 class="text-center">@ViewData["Title"]</h2>

<div class="row">
    <div class="col-sm-12 offset-lg-2 col-lg-8 offset-xl-3 col-xl-6">
        <form method="post">
            <div class="mb-3">
                <label asp-for="Name">Sub Category Name:</label>
                <input type="text" class="form-control" asp-for="Name" disabled>
            </div>
            <div class="mb-3">
                <label asp-for="CategoryId"></label>
                <select asp-for="CategoryId" class="form-control" disabled>
                    @foreach (var category in Model.Categories)
                    {
                        <option value="@category.Id">@category.Name</option>
                    }
                </select>
            </div>
            
            <div class="text-center">
                <button type="submit" class="btn btn-danger mt-3 w-100" value="Save">Delete</button>
            </div>
            
        </form>
    </div>
</div>